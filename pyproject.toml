[project]
name = "audux"
version = "1.0.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "fastapi==0.141.1",
    "uvicorn==0.52.4",
    "sqlmodel==0.0.42",
    "pydantic==2.13.5",
    "mutagen==1.48.1",
    "python-multipart==0.0.32",
    "httpx==0.28.1",
    "cryptography==50.0.1",
    "numpy==2.5.2",
    "onnxruntime==1.29.0",
]

[project.optional-dependencies]
asr = [
    "faster-whisper==1.2.1",
]

[dependency-groups]
build = [
    "pyinstaller==6.22.2",
    "pyinstaller-hooks-contrib==2026.7",
]
test = [
    "mypy>=2.3.1",
    "pytest==9.1.1",
    "pytest-cov==7.1.0",
    "ruff>=0.16.5",
]

[tool.pytest.ini_options]
testpaths = ["backend/tests"]
pythonpath = ["backend"]

[tool.coverage.run]
branch = true
source = ["app"]

[tool.coverage.report]
fail_under = 70
precision = 1
show_missing = true
skip_covered = true

[tool.coverage.xml]
output = "coverage.xml"

[tool.ruff]
target-version = "py312"

[tool.ruff.lint]
# Start with correctness rules that are safe for SQLAlchemy expressions and
# existing compatibility re-exports. Broaden this set as legacy findings are
# addressed instead of landing a repository-wide mechanical rewrite.
select = ["E9", "F63", "F7", "F82"]

[tool.mypy]
python_version = "3.12"
warn_unused_configs = true
check_untyped_defs = true

[tool.uv]
package = false
